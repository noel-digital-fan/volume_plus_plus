<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <!-- Media-output speaker, fully outlined (line-art). The cone is hollow with an INNER 2.5 border:
         it's stroked at double width (5) and clipped to its own silhouette, so only the inside half of
         the stroke shows and the outer edge stays flush with the original outline (the border grows
         inward, never outward). The two sound waves are drawn as arc strokes of the same 2.5 weight so
         they read as borders too. Tinted at runtime, so the colours here are placeholders; adapts to
         light/dark automatically. -->
    <group>
        <clip-path android:pathData="M3,9v6h4l5,5L12,4L7,9L3,9z" />
        <path
            android:fillColor="#00000000"
            android:strokeColor="#FF000000"
            android:strokeWidth="4"
            android:strokeLineJoin="round"
            android:strokeLineCap="round"
            android:pathData="M3,9v6h4l5,5L12,4L7,9L3,9z" />
    </group>
    <path
        android:fillColor="#00000000"
        android:strokeColor="#FF000000"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:pathData="M14.3,8.5a4.3,4.3 0 0 1 0,7" />
    <path
        android:fillColor="#00000000"
        android:strokeColor="#FF000000"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:pathData="M14,4.5a7.7,7.7 0 0 1 0,15" />
</vector>
